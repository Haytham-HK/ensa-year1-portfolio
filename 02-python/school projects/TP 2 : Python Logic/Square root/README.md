# Square Root Approximator

## Definition

A Python script that finds the approximate integer square root of a positive number. It uses a binary search (bisection) algorithm to find the result.

## How to Run

1.  Run the script from your terminal:
    ```bash
    python square_root.py
    ```

2.  Enter a positive number when prompted.