# TP03 — Python: Palindrome Sequence

This Python exercice implements an algorithm to find a palindrome. It takes a positive integer from the user, and if it's not a palindrome, it repeatedly adds the sum of its own digits to itself until it becomes one.

## Prerequisites
- Python 3.x installed.